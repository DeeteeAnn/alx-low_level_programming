#include <stdio.h>
/**
 *main - Entry point
 *Description: 'list all single digit numbers from base ten'
 *Return: Always 0
 */
int main(void)
{
	int m;

	for (m=0; m <=9; m++)
	{
		printf("%d", m);
	}
	printf("\n");
        return (0);
}
